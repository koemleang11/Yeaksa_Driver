import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const TotalAssignedScreen = () => {
  return (
    <View>
      <Text>TotalAssignedScreen</Text>
    </View>
  );
};

export default TotalAssignedScreen;

const styles = StyleSheet.create({});
